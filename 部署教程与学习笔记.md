# Real-Time-Fall-Detection 部署教程与学习笔记

## 一、项目简介

基于 **YOLOv8-Pose** 的实时人体跌倒检测系统。支持**单摄像头**和**双摄像头**两种模式，通过摄像头或视频输入检测人体姿态，融合规则判断与物理特征进行跌倒检测。

- GitHub: https://github.com/Macchiato-7782/-YOLOv8-Pose-
- 技术栈: YOLOv8-Pose + OpenCV + NumPy + PyTorch
- 代码量: 6 个模块，约 1000 行
- 是否需要训练: 不需要，纯规则 + 物理特征判断

---

## 二、完整部署教程（macOS）

### 2.1 环境要求

| 项目 | 要求 |
|------|------|
| 系统 | macOS (本教程) / Windows / Linux |
| Python | 3.8+（本机 3.13） |
| 硬件 | 有摄像头（内置或外接） |
| 网络 | 首次需要下载依赖和模型 |

### 2.2 克隆项目

```bash
git clone https://github.com/Macchiato-7782/-YOLOv8-Pose-.git
cd -YOLOv8-Pose-
```

### 2.3 创建虚拟环境

```bash
python3 -m venv venv
```

虚拟环境的作用：将项目的依赖与系统 Python 隔离，避免版本冲突。

```
项目目录/
├── main.py              # 入口
├── fall_logic.py        # 跌倒判断逻辑
├── features.py          # 物理特征计算
├── tracking.py          # 单摄跟踪
├── cross_camera.py      # 跨摄像头匹配
├── camera_process.py    # 摄像头处理进程
├── test_fall_detection.py  # 模拟测试
├── requirements.txt
└── venv/                ← 虚拟环境
    ├── bin/python3
    ├── bin/pip
    └── lib/python3.13/
        └── site-packages/
```

### 2.4 激活虚拟环境

```bash
source venv/bin/activate
```

激活后终端提示符会多一个 `(venv)` 前缀。每次打开新终端都需要重新激活。

### 2.5 安装依赖

```bash
# 使用清华镜像源（国内加速）
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple --trusted-host pypi.tuna.tsinghua.edu.cn
```

安装的依赖清单：

| 包名 | 用途 | 大小 |
|------|------|------|
| opencv-python | 图像处理、摄像头读取、窗口显示 | ~46MB |
| numpy | 数值计算 | ~5MB |
| ultralytics | YOLOv8 框架 | ~1.2MB |
| torch (间接) | PyTorch 深度学习框架 | ~80MB |
| torchvision (间接) | 视觉模型工具 | ~2MB |

### 2.6 模型文件

首次运行时，ultralytics 会自动下载 `yolov8n-pose.pt`（约 6MB）。

模型命名规则：
- `yolov8` — YOLO 第 8 版
- `n` — nano 版本（最小最快，还有 s/m/l/x 更大更准的版本）
- `pose` — 姿态估计任务
- `.pt` — PyTorch 格式

### 2.7 macOS 摄像头权限

首次使用摄像头需要授权：

```
系统设置 → 隐私与安全性 → 摄像头 → 打开"终端(Terminal)"的开关
```

如果列表里没有"终端"：
1. 点 `+` 号
2. 按 `Cmd+Shift+G`
3. 输入 `/Applications/Utilities/`
4. 选择 `Terminal.app`
5. 打开开关

### 2.8 关于 Continuity Camera

macOS 的"连续互通相机"功能会让 iPhone 自动成为 Mac 的外接摄像头。如果想用 Mac 内置摄像头：

- 方法 1: 把 iPhone 拿远或关掉蓝牙
- 方法 2: 运行时指定 `--cam_ids 1`（默认是 0，即 iPhone）

### 2.9 运行

```bash
# 单摄像头（默认 cam_id=1）
python main.py

# 指定摄像头 ID
python main.py --cam_ids 0

# 单视频文件
python main.py --video your_video.mp4

# 双摄像头
python main.py --num_cams 2 --cam_ids 0 1

# 双视频文件
python main.py --num_cams 2 --video cam1.mp4 cam2.mp4

# 保存输出视频
python main.py --num_cams 2 --cam_ids 0 1 --save_output
```

按 ESC 退出。

### 2.10 运行测试

不依赖摄像头，用合成数据验证检测逻辑：

```bash
# 运行全部测试（53 个用例）
python -m pytest test_*.py -v

# 只运行跌倒场景测试
python -m pytest test_fall_detection.py -v
```

覆盖 4 个测试模块：跌倒场景、物理特征计算、跟踪器、跨摄像头匹配。

---

## 三、项目架构

### 3.1 文件结构

```
├── main.py              # 入口，支持单/双摄像头模式
├── fall_logic.py        # 融合跌倒判断逻辑（四路检测 + 滑动窗口 + 持续时间）
├── features.py          # 物理特征计算（旋转能量、重力因子、头部下降）
├── tracking.py          # ByteTracker 跟踪 + 幽灵机制 + 跌倒状态继承
├── cross_camera.py      # 跨摄像头匹配（HSV 直方图 + 稳定婚姻算法）
├── camera_process.py    # 摄像头处理进程（多进程架构）
├── config.py            # 配置加载器
├── config.yaml          # 所有可调参数（阈值、窗口、超时等）
├── test_fall_detection.py  # 跌倒场景测试（7 个场景）
├── test_features.py     # 物理特征测试
├── test_tracking.py     # 跟踪器测试
├── test_cross_camera.py # 跨摄像头匹配测试
├── requirements.txt     # 依赖清单
└── yolov8n-pose.pt      # YOLO 模型（首次运行自动下载）
```

### 3.2 单摄像头工作流

```
摄像头 → 逐帧读取 → YOLOv8-Pose (17关键点, conf=0.3)
    → extract_detections(): 过滤、计算 bbox/宽高比/5关键点
    → ROI 二次推理（对潜在跌倒区域再检测一次，过滤假检）
    → 合并去重（IoU > 0.9 去重）
    → 质心跟踪 update()
    → extract_angle_keypoints(): 提取肩/髋/膝坐标
    → evaluate_fall(): 四路跌倒判断
    → 绘制标注 → 显示
```

### 3.3 双摄像头工作流

```
摄像头A ─→ 进程A: YOLO检测 + 跟踪 + 直方图 + 角度关键点 ──→ Queue ──┐
                                                                      ├──→ 主进程:
摄像头B ─→ 进程B: YOLO检测 + 跟踪 + 直方图 + 角度关键点 ──→ Queue ──┘
                                                                     ├─ 稳定婚姻匹配
                                                                     ├─ fall_state 持久化
                                                                     ├─ 四路跌倒判断
                                                                     ├─ 双视角交叉验证
                                                                     └─ 拼接显示
```

关键设计：
- 每个摄像头独立进程，通过 `mp.Queue(maxsize=1)` 传递数据
- 队列非阻塞，丢帧不卡死
- 主进程用 `fall_state_store` 跨帧持久化跌倒状态
- 跨摄像头匹配用 HSV 直方图 + 稳定婚姻算法

---

## 四、核心知识点

### 4.1 YOLOv8-Pose 姿态估计

YOLOv8 是 Ultralytics 公司开发的目标检测模型，Pose 版本在检测人体的同时输出 17 个关键点：

```
COCO 17 关键点：
0  - Nose (鼻子)
1  - LEye (左眼)
2  - REye (右眼)
3  - LEar (左耳)
4  - REar (右耳)
5  - LShoulder (左肩)
6  - RShoulder (右肩)
7  - LElbow (左肘)
8  - RElbow (右肘)
9  - LWrist (左腕)
10 - RWrist (右腕)
11 - LHip (左髋)
12 - RHip (右髋)
13 - LKnee (左膝)
14 - RKnee (右膝)
15 - LAnkle (左踝)
16 - RAnkle (右踝)
```

每个关键点包含 `(x, y, confidence)` 三个值。

### 4.2 "全身可见"过滤

不是所有检测都可靠——如果一个人只露出上半身，关键点缺失，姿态判断就不准。

代码中定义了 7 个必要关键点 `[0, 5, 6, 11, 12, 15, 16]`（鼻子、双肩、双髋、双踝），至少要有 4 个可见且置信度 > 0.2，才算"全身可见"，才进入跌倒判断逻辑。

### 4.3 ByteTracker 跟踪

v2 使用 Ultralytics 内置 ByteTracker（卡尔曼滤波 + 级联匹配），替代手写匈牙利匹配：

```
1. model.track(tracker="bytetrack.yaml") → 自动分配 track_id
2. 提取检测结果：bbox、关键点、宽高比
3. 更新 person_history：已有 track 更新状态，新 track 创建初始状态
4. 幽灵机制：目标消失后保留跌倒状态（普通 3 秒，跌倒 5 秒）
5. 跌倒状态继承：新 track_id 与幽灵目标 IoU > 0.2 → 继承 fall_state
```

### 4.4 四路跌倒检测

系统用四条独立路径检测跌倒，任一路径触发即标记为"可能跌倒"：

#### 路径 1: 几何检测（AR + 角度）

| 规则 | 条件 | 含义 |
|------|------|------|
| 宽高比 | AR / 初始AR < 0.35 | 人变得"扁"了（比站姿扁 65%） |
| 髋部角度 | 肩-髋-膝夹角 < 130° | 身体折叠 |

两个条件必须**同时满足**才触发（AND 逻辑，减少坐姿误报）。

#### 路径 2: 物理检测（RE / GF / 头部下降）

| 特征 | 阈值 | 含义 |
|------|------|------|
| 旋转能量 (RE) | > 8 rad/s | 倒立摆模型，身体绕脚旋转的角速度 |
| 重力因子 (GF) | > 8000 pixel/s² | 重心加速度方向与重力的一致性 |
| 头部下降 (HD) | > 0.2 | 头部下降量 / 身高 |

任一特征超过阈值即触发，但**需要几何确认**（AR 变化 > 10% 或头部下降），防止走路/弯腰误报。

#### 路径 3: 侧倒检测（AR 剧变 + 头部下降）

- AR 比初始值下降 65% 以上（ar_ratio < 0.35）
- **且** 头部持续下降 > 身高的 20%

用于捕捉侧倒（角度可能不够低，但 AR 变化剧烈）。

#### 路径 4: 已在地面检测

- 初始 AR 就很低（非站立状态，AR < 1.0）
- 当前 AR < 0.4（极低，排除弯腰/半蹲）
- ar_low_frames 累积 >= 10 帧

用于检测被摄像头启动时已经在地上的人。

### 4.5 滑动时间窗口

```python
window_size = 20           # 窗口大小（帧数）
window_trigger_ratio = 0.5 # 窗口内 50% 帧触发即可
min_consecutive_triggers = 5 # 连续触发帧数要求（允许 1 帧间隙）
```

不是每帧都触发才算跌倒，而是在一个滑动窗口内有足够比例的帧触发，且需要连续触发。连续触发允许 1 帧间隙（`gap_count` 机制），防止倒地过程中检测框抖动导致计数归零。

### 4.6 持续时间确认

```python
min_fall_pose_duration = 3.5  # 必须持续 3.5 秒才确认跌倒
```

排除蹲下、弯腰等动作。3.5 秒给弯腰恢复留足够时间：弯腰触发潜在跌倒后，如果在 3.5 秒内检测到头部回升（回弹检测），就取消跌倒判定。

### 4.7 跌倒状态粘性

一旦确认跌倒（`fall_detected = True`），状态不会因为物理信号消失而重置。人躺在地上时 RE/GF 会降为 0，但跌倒状态保持。

只有检测到明确恢复证据（AR 恢复到站立基线的 70% 以上）才重置。

### 4.8 五状态模型

```
Normal（正常）
    ↓ 四路检测任一触发 + 滑动窗口 50% 通过 + 连续 5 帧
Potential Fall（可能跌倒）← 橙色标注，开始计时
    ↓ 持续 >= 3.5 秒 且 无回弹
FALL（确认跌倒）← 红色标注，状态粘性
    ↓ AR 恢复到站立基线 70%
Normal（恢复正常）← 绿色标注
```

### 4.9 高置信度快速通道

当以下四个条件**同时满足**时，跳过持续时间要求，立即确认跌倒：

- AR < 0.6（非常扁）
- 角度 < 120°（严重折叠）
- RE > 15（高旋转能量，rad/s）
- GF > 15000（高重力因子，pixel/s²）

### 4.10 ROI 二次推理

对可能跌倒的区域（低宽高比），在更小的裁剪区域内用更低的置信度再推理一次。这可以：
- 找回主推理漏掉的躺卧人体
- 过滤假检（IoU 匹配要求）
- 控制耗时（最大 0.1 秒超时）

### 4.11 双摄像头交叉验证

| 情况 | 置信度 | 含义 |
|------|--------|------|
| 两个摄像头都确认跌倒 | 95% | 高置信度 |
| 只有一个确认 | 60% | 低置信度警告 |

跨摄像头匹配用 HSV 直方图 + 稳定婚姻算法（Gale-Shapley）。

### 4.12 关键参数（v2，集中配置在 config.yaml）

| 参数 | 值 | 含义 |
|------|-----|------|
| `horizontal_ar_threshold` | 0.6 | 宽高比阈值 |
| `angle_threshold` | 120° | 髋部角度阈值 |
| `min_fall_pose_duration` | 3.5s | 跌倒确认时间 |
| `re_threshold` | 8 | 旋转能量阈值 (rad/s) |
| `gf_threshold` | 8000 | 重力因子阈值 (pixel/s²) |
| `head_descent_threshold` | 0.2 | 头部下降阈值 |
| `window_size` | 20 | 滑动窗口帧数 |
| `window_trigger_ratio` | 0.5 | 窗口触发比例 |
| `min_consecutive_triggers` | 5 | 连续触发帧数要求 |
| `rebound_threshold` | 0.15 | 头部回弹阈值 |
| `rebound_frames` | 2 | 回弹确认帧数 |
| `min_standing_ar` | 1.0 | 站立 AR 最低值 |
| `ghost_timeout` | 3.0s | 幽灵目标超时 |
| `ghost_timeout_fallen` | 5.0s | 已跌倒目标幽灵超时 |

### 4.13 与 HumanFallDetection 项目的对比

| 维度 | HumanFallDetection | 本项目 |
|------|-------------------|--------|
| 姿态估计 | OpenPifPaf (已停更) | YOLOv8-Pose (活跃维护) |
| 跟踪 | 自写匈牙利匹配 | ByteTracker (卡尔曼滤波 + 级联匹配) |
| 分类方法 | LSTM 神经网络 | 纯规则 + 物理特征 |
| 是否需要训练 | 需要 | 不需要 |
| 特征数量 | 5 个物理特征 | 4 路检测（几何+物理+侧倒+地面） |
| 物理特征降噪 | 无 | EMA + Savitzky-Golay 双层平滑 |
| 多摄像头 | 支持 | 支持（多进程 + 跨摄像头匹配） |
| 状态管理 | 无 | fall_state 跨帧持久化 + 粘性 + 幽灵继承 |
| 参数配置 | 代码内硬编码 | config.yaml 集中配置 |
| 部署难度 | 高（Python 版本限制） | 低 |

### 4.14 该领域的技术演进

```
2018-2020: OpenPose + SVM/Random Forest
2020-2022: OpenPifPaf + LSTM / TCN
2022-2024: YOLOv8-Pose + ST-GCN / Transformer
2025-2026: YOLOv8-Pose + 轻量 Transformer / 端到端模型
```

---

## 五、调试过程中解决的关键问题

### 5.1 numpy view 跨进程序列化数据丢失

**现象**: 双摄模式下 `angle_keypoints` 全部为 `(0.0, 0.0)`，角度计算为 0°。

**根因**: `pose.xy[0].cpu().numpy()` 返回的 numpy array 与底层 torch tensor 共享内存。torch tensor 在 YOLO 推理结束后被垃圾回收，numpy array 指向已释放的内存。

**修复**:
- `tracking.py`: `keypoints = pose.xy[0].cpu().numpy().copy()`
- `camera_process.py`: `float(keypoints[idx][0])` 转为纯 Python float
- `fall_logic.py`: `float(np.mean(...))` 转为纯 Python float

### 5.2 双摄模式 fall_state 每帧重置

**现象**: 跌倒检测在单摄模式正常，双摄模式永远不触发。

**根因**: `camera_process` 每帧通过 Queue 传来新的 `fall_state` dict，主进程修改的状态在下一帧被覆盖。`trigger_history`、`fall_start_time` 等无法跨帧累积。

**修复**: 主进程添加 `fall_state_store` 字典，evaluate_fall 前恢复、后保存。

### 5.3 跌倒确认后状态重置

**现象**: 跌倒检测到 FALL 后，几秒内自动恢复为 Normal。

**根因**: 人躺在地上时物理信号（RE/GF）消失，滑动窗口触发率降到 50% 以下，`is_potential_fall` 被重置，`fall_detected` 也跟着重置。

**修复**: 已确认跌倒后跳过滑动窗口重置逻辑，只在检测到恢复（AR 回升）时才重置。

### 5.4 initial_ar 保护过于严格

**现象**: 摄像头启动时人在坐姿，后续跌倒检测被永久屏蔽。

**根因**: `initial_ar` 记录了坐姿时的 AR（如 0.8），后续 AR 变化检测被跳过。

**修复**: `initial_ar_low` 标志只屏蔽 AR 相关路径，物理检测和头部下降路径仍然有效。

---

## 六、常见问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| `ModuleNotFoundError: cv2` | 没激活虚拟环境 | `source venv/bin/activate` |
| `ModuleNotFoundError: lap` | 缺少 ByteTracker 依赖 | `pip install lap` |
| `not authorized to capture video` | macOS 摄像头未授权 | 系统设置 → 隐私与安全性 → 摄像头 |
| 用的是手机摄像头 | Continuity Camera | 把 iPhone 拿远或 `--cam_ids 1` |
| 检测不到人 | 距离太远/光线暗 | 降低 `min_conf_kpt`，或走近摄像头 |
| 弯腰误报 | 物理特征阈值太松 | 提高 `re_threshold` 和 `gf_threshold` |
| 跑动闪报 | 物理路径缺少几何确认 | 已内置几何守卫（AR 变化 > 10%） |
| 坐着被误判为跌倒 | 角度阈值不够严格 | 系统已用 AND 逻辑（AR + 角度同时触发） |
| 躺着不被检测 | 物理信号不够 | 检查 RE/GF 阈值，或增加 ROI 二次推理 |
| 跌倒后检测框消失再出现显示正常 | ByteTracker 分配新 ID | 已内置幽灵状态继承（IoU 匹配） |

---

## 七、v2 新增知识点

### 7.1 ByteTracker 跟踪

v1 用手写匈牙利匹配做跟踪（每帧计算距离矩阵 + scipy.optimize.linear_sum_assignment），v2 改用 Ultralytics 内置 ByteTracker。

ByteTracker 的核心改进：
- **卡尔曼滤波**：预测目标下一帧位置，即使 YOLO 漏检也能保持跟踪
- **级联匹配**：先匹配高置信度检测，再用低置信度检测补充
- **运动模型**：速度、加速度建模，匹配更准确

集成方式：`model.track(tracker="bytetrack.yaml")`，跟踪 ID 在 `results.boxes.id` 中。

### 7.2 幽灵目标跌倒状态继承

ByteTracker 在目标短暂丢失后可能分配新的 track_id。如果跌倒状态不继承，检测框消失再出现就会显示"Normal"。

解决：新 track_id 出现时，检查是否有重叠的幽灵目标携带 `fall_detected=True`，通过 IoU 匹配继承其 `fall_state`。

```python
def _find_ghost_fall_state(self, new_bbox):
    for tid, data in self.person_history.items():
        if not data.get('is_ghost', False):
            continue
        fs = data.get('fall_state', {})
        if not fs.get('fall_detected', False):
            continue
        iou = self._compute_iou(new_bbox, data['bbox'])
        if iou > 0.2:
            del self.person_history[tid]
            return fs
    return None
```

### 7.3 Savitzky-Golay 滤波

物理特征（RE/GF）在实际视频中噪声很大。v1 只用原始值，v2 加了两层平滑：

1. **EMA（指数移动平均）**：`ema = α * raw + (1-α) * prev_ema`，α=0.3
2. **Savitzky-Golay 滤波**：在 EMA 基础上再做二次平滑，保留信号形状

SG 滤波比纯 EMA 更好：EMA 会让尖峰变宽（延迟增大），SG 保留尖峰形状同时去噪。参数：窗口 7 帧，二阶多项式。

### 7.4 物理路径几何守卫

v1 的物理路径（RE/GF 超阈值）独立触发，走路、弯腰时容易误报。

v2 加了几何守卫：物理路径触发需配合几何信号（AR 变化 > 10% 或头部下降），否则不计入 `is_potential_fall`。

```python
ar_changed = ar_ratio < 0.9  # AR 下降超 10%
physics_with_geometry = physics_triggered and (ar_changed or head_drop_triggered)
```

效果：
- 跑动：AR 不变 → 物理信号被过滤
- 浅弯腰：AR 变化 < 10% → 物理信号被过滤
- 深弯腰：AR 变化 > 10% → 物理信号计入，但 3.5 秒持续时间 + 回弹检测可取消
- 真跌倒：AR 剧变 → 物理信号正常计入

### 7.5 帧率归一化

v1 的 RE/GF 阈值是按帧算的，不同帧率的视频需要不同阈值。v2 归一化为每秒值：

- RE：rad/s（每秒旋转弧度）
- GF：pixel/s²（每秒加速度）

通过 `dt = current_time - prev_frame_time` 计算帧间时间，传给特征计算函数。

### 7.6 参数调优经验

| 参数 | 调优方向 | 原因 |
|------|----------|------|
| `min_fall_pose_duration` | 增大（3.5s） | 给弯腰恢复留时间 |
| `rebound_threshold` | 降低（0.15） | 更容易检测到弯腰恢复 |
| `min_consecutive_triggers` | 增大（5帧） | 过滤瞬间动作 |
| `re_threshold` / `gf_threshold` | 增大（8/8000） | 过滤走路/弯腰小动作 |
| `angle_threshold` | 降低（120°） | 更严格的三点角度要求 |
| `window_size` | 增大（20帧） | 更大的观察窗口 |

---

## 八、下一步学习方向

1. **换用自己的视频测试** — `python main.py --video 你的视频.mp4`
2. **调参** — 修改 `config.yaml` 中的阈值，观察效果变化
3. **加报警功能** — 检测到跌倒时发送通知（邮件/微信/蜂鸣器）
4. **运行测试** — `python -m pytest test_*.py -v` 验证逻辑正确性
5. **了解 ST-GCN** — 图卷积网络，比规则判断更准确的时序分类方法
6. **部署到边缘设备** — 树莓派、Jetson Nano 等嵌入式设备上运行
